Things to do to get running on your own machine:
- (edit and execute) ./bs/boostrap.sh to install PEAR and PECL libraries and build samples
- add extensions to the php.ini file
- put samples/www somewhere in web path of apache
- add .htaccess information to <Directory> in http.conf or make sure AllowOverrides is set (to use htaccess) for samples

Things to do to get running on EC2:
- TBD
